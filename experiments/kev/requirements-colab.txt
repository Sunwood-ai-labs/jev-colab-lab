# Runtime dependencies for the isolated Colab VM.
# torch is normally preinstalled with the selected CUDA runtime; the official
# kev constraints are kept here so uv can resolve compatible Python packages.
accelerate>=1.15,<2
huggingface-hub>=0.30,<1
numpy>=2.5,<3
peft>=0.15,<1
pydantic>=2.7,<3
pygame>=2.6,<3
torchao>=0.16,<1
transformers>=4.51,<4.58
